// import React from "react";

// export const getItems = (count, offset = 0) =>
// Array.from({ length: count }, (v, k) => k).map(k => ({
//   id: `item-${k + offset}`,
//   header: `Task ${k + offset}`,
//   content: `Test it ${k + offset} times`
// }));